/**
 * Deprecated: the per-topic/subtopic resource uploader was replaced by the owner-owned notes library.
 * Upload via NoteUploadDialog (library or with a parent node) — see components/educator/note-upload-dialog.
 * Re-exported here only so any lingering import keeps resolving.
 */
export { NoteUploadDialog as ResourceUploadDialog } from "@/components/educator/note-upload-dialog";

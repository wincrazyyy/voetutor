/** Public support / contact email — single source of truth. Referenced by the legal pages
 *  (privacy, terms) and anywhere else that surfaces a contact address, so it is defined once. */
export const SUPPORT_EMAIL = "support@wsmath.com";
